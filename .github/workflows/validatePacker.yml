name: Packer Validation

on:
  push:
    branches:
        - A04-01

jobs:
  build:
    name: Packer Status Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup packer
        uses: hashicorp/setup-packer@main
        id: setup
        with:
          version: latest

      - name: Install plugins
        run: packer init packer/aws_ami.pkr.hcl

      - name: Run packer fmt
        run: packer fmt -check packer/aws_ami.pkr.hcl

      - name: Run packer validate
        run: |
          packer validate packer/aws_ami.pkr.hcl